import React from 'react';

const UpdateItemTwoProperties = (first, firstValue, second, secondValue) => {
    return {
            [first]: firstValue,
            [second]: secondValue
        };
};

export default UpdateItemTwoProperties;